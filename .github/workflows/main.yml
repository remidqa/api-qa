name: Build and Deploy Docker Image

on:
  workflow_dispatch:
  #push:
  #  branches:
  #    - main

env:
  APP: api-qa2
  ENV: preview

jobs:
  execute-tests:
    uses: remidqa/use-guthubactions/.github/workflows/main.yml@main
    with:
      APP: ${{ env.APP }}
      ENV: ${{ env.ENV }}
